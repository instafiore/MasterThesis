1) controllare questa cosa del prefisso J che soddisfa la condizione
2) la ragione con il parametro l in più 
3) defining in propagation section what is mul (maximum undefined)
4) introdurre concetto di 'variabile'